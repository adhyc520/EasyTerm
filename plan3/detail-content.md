# 附件：内容类应用细节（Content Apps）

> browser / editor 全量发现（file:line + 修复）。对应主方案 §6。
> 共性：二者 tab 条几乎一样（横向 `ListView.builder`、14px 关闭图标、无重排、无键盘、8 上限）-> 抽 `DesktopTabStrip`（B.3）一次修两边。

---

## B. Browser（`lib/desktop/apps/browser_app.dart`，1374 行）

### Tab 管理
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| B1 | 全文（仅 `:1066` enter 绑定） | 无快捷键：Ctrl+T/W/R/L、Ctrl+Tab、Ctrl+1..8 | `CallbackShortcuts`+`Focus` 绑定 |
| B2 | `:849-956` | tab 不可重排/固定/复制 | `DesktopTabStrip`（Reorderable + 右键菜单：固定/复制/关闭其他/关闭右侧） |
| B3 | `:878` | tab 溢出无滚动条/计数 | 滚动条 + 活动自动滚入视 + `N/8` 计数 |
| B4 | `:924-934` | 关闭图标 14px，命中区 ~18px | `DesktopTabStrip` ≥28×28 |
| B5 | `:414-428` | 末 tab 关闭静默重置（editor 关窗） | 统一：关窗 or 显「已重置」提示（与 editor 对齐，主方案 §6） |
| B6 | `:369-393,395-410` | 弹窗达 8 上限静默替换 LRU 后台 tab | 提示「8 tabs，替换最旧非活动 tab？」或临时提限 + 闪被换 tab |

### 地址栏
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| B7 | `:757-762` | 聚焦不全选，需手动清旧 URL | 聚焦 `selection = TextSelection(0, text.length)` |
| B8 | `:1061-1128` | 无安全指示（HTTPS/网关/直连） | 前缀锁/解锁图标 + tooltip 解释网关 vs 直连 vs 公网 |
| B9 | `:190-199,1280-1325` | 建议仅 pick，无 inline 自动补全 | 首匹配填入并选中追加部分（ghost text） |
| B10 | 工具栏 | 无「粘贴并跳」「复制当前 URL」 | 后缀操作 + 右键菜单 |
| B11 | `:1072` | 断线时地址栏整字段 disabled | 字段保持可输（备 URL），仅提交动作禁用 |

### 错误/加载
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| B12 | `:71,687-692,802-812` | 错误全局单字段，跨 tab 串味 | `error` 移入 `_BrowserTab`，只渲染活动 tab（主方案 C5） |
| B13 | `:802-812` | 错误条无重试/关闭 | 加「重试」「X」 |
| B14 | webview 区 | 无真错误页（404/DNS/拒绝） | 活动 tab 错误态渲染居中错误组件（状态码/host/重试/系统浏览器打开） |
| B15 | `:339-345,654-672` | 地址栏聚焦时加载指示被抑制 | 加载态用独立 `ValueNotifier`，不碰 TextField composition |

### 缺失惯例
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| B16 | 全文 | 无 find-in-page | `FindInteractionController`，Ctrl+F 开查找栏，next/prev |
| B17 | 全文 | 无缩放/持久 | Ctrl+/-/0 + 每主机持久 |
| B18 | 全文 | 无下载处理（无 `onDownloadStartRequest`） | 注册下载，存本地/SFTP，显 toast/进度 |
| B19 | 全文 | 无 DevTools 入口 | 溢出菜单「开发者工具」（平台支持时） |
| B20 | `:357` | 新 tab 空白，无常用站点页 | `currentUrl==null` 渲染轻量新标签页（历史常用 + 书签） |
| B21 | `:1214-1261` | 历史仅「清空」，无逐项删除 | 逐项删除 IconButton（对齐书签菜单） |
| B22 | `browser_bookmarks_store.dart` 全文 | 书签仅 URL，无标题/分组/重排 | 存 `{title,url}`，可重命名，按分组 |
| B23 | `:699-703,626,627` | 自签证书/混合内容静默放行无指示 | 自签时地址栏显「证书未验证」；考虑首次按主机警告 |
| B24 | `:1262-1276` | 模式切换无 tooltip/图标 | tooltip「切换访问方式：SSH 网关 / 本地端口转发」+ `Icons.swap_horiz` |
| B25 | `:778-801` | JS 提示横幅占垂直空间且跨会话复现 | 改模式切换一次性 tooltip |

---

## E. Editor（`lib/desktop/apps/editor_app.dart`，996 行）

### 编辑面
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| E1 | `:965-994` | 无行号（语法错误条还报行号） | 左侧 gutter 渲染行号，随 TextField 滚动同步 |
| E2 | `:608-653` | Tab 移出焦点不缩进；Enter 无自动缩进 | 拦截 Tab（插空格/tab，保缩进）、Shift+Tab 反缩进、Enter 复制当前行前导空白 |
| E3 | `editor_highlight.dart:143-145,218-220` | HTML 校验但不高亮 | 实现 `_highlightHtml`（标签/属性/串/注释） |
| E4 | `editor_syntax.dart:34-67` | TS/Py/Go/C/CSS/SQL/XML/MD 等回落 plain | `.ts/.tsx` 走 JS 高亮；补 CSS/Py/XML/MD/Dockerfile/ini 轻量高亮 |
| E5 | `:969` | 长行强制换行无开关 | 工具栏换行开关；关时横向滚动 |
| E6 | `:373,416` | 硬编码 UTF-8，损坏非 UTF-8 文件；不识 BOM/CRLF | 编码检测；状态栏显编码；「重新以编码打开」「保存为编码」；保留 CRLF/LF；去 BOM（主方案 C4） |

### 查找/替换
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| E7 | `:441-460` | 命中不全量高亮，仅当前 | 覆盖层高亮所有 `_findHits` |
| E8 | `:830-833` | 无匹配无反馈 | 显「无匹配」/「0/0」，查找框转红 |
| E9 | `:453` | 仅大小写敏感 `indexOf` | 大小写/正则/整词切换 |
| E10 | `:675-679` | 切 tab 查找偏移未重建 -> 选错区间 | 切 tab 后 `_rebuildFindHits()`+`_selectFindHit()`（主方案 C6） |
| E11 | `:608-653,819` | Esc 不关查找；Shift+Enter 不上一个 | 绑 Esc 关查找；Shift+Enter 上一个 |
| E12 | `:767-770 vs 614-625` | 工具栏图标 toggle 与 Cmd+F 仅 open 不一致 | 二者统一 toggle |
| E13 | `:522-571` | 跳行对话框无当前/总行 | 显「当前 N / 共 M 行」 |
| E14 | `:924-937` | 语法错误条不可点跳行 | banner 可点跳到 `syntaxIssue.line` |

### 文件/跨应用
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| E15 | `:732-790` | 无「打开」按钮（仅拖入/来自 file_manager） | 「打开」-> 远端路径选择（复用 SFTP browser）或绝对路径输入 |
| E16 | `:411` | 仅存回原路径，无另存为/新草稿 | 「另存为」输远端路径重绑 `tab.path`；允许未命名草稿 tab |
| E17 | `:300-302` | 末 tab 关闭即关窗 | 回空「拖入文件以打开」态（或设为偏好） |

### 远程变更
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| E18 | `:398-409,916-918` | 「忽略」不生效（3s 复现） | 忽略时 `tab.remoteMtime = t` 作基线 + `ignoredMtime`，进一步变更仍告警（主方案 C3） |
| E19 | `:399` | 只轮询活动 tab，后台 tab 远程被改不告警 -> 覆盖风险 | `_checkRemote` 遍历所有 tab（节流）（主方案 C3） |

### Tab 管理
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| E20 | `:661-731,608-653` | 无重排/键盘切换/Ctrl+W | `DesktopTabStrip` + Ctrl+W 关、Ctrl+Tab/Cmd+Alt+←→ 切换 |
| E21 | `:713-720` | 关闭图标 14px | `DesktopTabStrip` ≥28×28 |
| E22 | `:661` | 死代码 `if (_tabs.length > 1 || true)` | 决定行为（单 tab 显隐）删死分支 |

### 状态/反馈
| ID | 位置 | 问题 | 修复 |
|---|---|---|---|
| E23 | `:741-752` | 无行:列/选区数 | 状态栏「行 N, 列 M」「已选 K 字符」 |
| E24 | `:428-435` | 保存成败仅 SnackBar 易漏 | 失败用持久条+重试；成功可改状态行「已保存 HH:MM」 |
| E25 | `:321` | 只读文件无标识，编辑后保存才失败 | `_load` stat 权限；不可写显「只读」徽标 + 字段只读或首次编辑警告 |

---

## 优先级（内容类内部）
**P0 数据/正确性**：E18 忽略不生效 · E19 后台 tab 不轮询 · E10 切 tab 查找错 · E6 编码损坏 · B12 错误跨 tab
**P1 核心摩擦**：E2 Tab 不缩进 · E1 行号 · B1 快捷键 · E15 打开文件 · B16 find-in-page · B7 全选 · B13/B14 错误重试/错误页
**P2 查找打磨**：E7 全量高亮 · E8 无匹配 · E9 大小写/正则 · E11 Esc/Shift+Enter · E13/E14 跳行上下文/可点
**P3 惯例/可发现**：B2/E20 重排 · B8 安全指示 · B24 模式 tooltip · E3/E4 高亮补全 · B17/B18/B19 缩放/下载/DevTools · E5 换行
**P4 打磨**：B4/E21 命中区 · B6 弹窗上限 · B21/B22 历史/书签 · E17 末 tab 关窗 · E22 死代码 · E23/E24/E25 状态/只读 · B5 末 tab 重置
